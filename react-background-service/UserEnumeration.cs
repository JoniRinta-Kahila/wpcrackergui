using System;
using System.Collections.Generic;
using System.Globalization;
using System.Linq;
using System.Net.Http;
using System.Text;
using System.Threading.Tasks;
using Newtonsoft.Json;

namespace react_background_service
{
    class UserEnumeration
    {
        //public static async void DoWork(int processId)
        //{
            //var processes = Program.ProcessList;
            //var myProcess = processes.First(x => x.Id == processId);
            
            //var client = new HttpClient();
            //var response = await client.GetAsync(myProcess.Url);
            //response.EnsureSuccessStatusCode();
            //var responseBody = await response.Content.ReadAsStringAsync();

            //var list = JsonConvert.DeserializeObject<List<UserObj>>(responseBody);

            //myProcess.TaskResult.UserEnumeration = list;
        //}
    }
}
